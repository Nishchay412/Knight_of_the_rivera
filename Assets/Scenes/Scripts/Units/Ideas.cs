using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// just ideas :: nothing else 

public class Ideas : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
